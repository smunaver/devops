ls /etc/netplan/
You’ll likely see something like:

00-installer-config.yaml
or

01-netcfg.yaml

Edit the file (example uses 00-installer-config.yaml):

sudo nano /etc/netplan/00-installer-config.yaml

network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:
      dhcp4: no
      addresses:
        - 192.168.0.8/24
      gateway4: 192.168.0.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4

sudo netplan apply
